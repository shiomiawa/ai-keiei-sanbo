# CLAUDE.md

このファイルは、Claude Code がこのプロジェクトで作業する際のガイドラインです。

## プロジェクト概要

- プロジェクト名: ai-keiei-sanbo(AI経営参謀)
- 概要: (プロジェクトの目的・概要をここに記載)
- 主な技術スタック: (使用する言語・フレームワークが決まったら記載)

## 基本ルール

- 応答・コメント・コミットメッセージは原則として日本語で書く。
- 既存コードの書き方(命名、コメントの量、インデント、スタイル)に合わせる。
- 依頼された範囲を超える変更やリファクタリングは行わない。
- 破壊的な操作(ファイル削除、履歴の書き換え、強制プッシュなど)は、事前にユーザーへ確認する。
- API キー・パスワードなどの秘密情報はコードやコミットに含めない。`.env` は `.gitignore` に追加する。

## Git 運用ルール

### 基本方針

**コードを変更するたびに、変更をコミットして GitHub にプッシュする。**
作業をまとめて溜め込まず、変更単位ごとにこまめにプッシュする。

### 手順

1. `git status` / `git diff` で変更内容を確認する。
2. 関係するファイルだけをステージする(`git add <file>`)。`git add -A` は、意図しないファイルや秘密情報が含まれないことを確認した上で使う。
3. 変更内容が分かるメッセージでコミットする。
4. `git push` で GitHub のリモートリポジトリへプッシュする。

### ブランチ

- `main` ブランチには直接コミットせず、作業用ブランチを切って作業する(例: `feature/xxx`, `fix/xxx`)。
- 作業ブランチも同様に、変更のたびに `git push`(初回は `git push -u origin <branch>`)する。
- 必要に応じて Pull Request を作成し、`main` へマージする。

### コミットメッセージ

- 1 行目に変更の要約を簡潔に書く(何を・なぜ変更したかが分かるように)。
- 必要なら空行を挟んで詳細を書く。
- 1 コミットには 1 つの目的の変更だけを含める。

### 禁止事項

- `git push --force` など履歴を書き換えるプッシュは、ユーザーの明示的な指示がない限り行わない。
- フック(pre-commit など)を `--no-verify` で回避しない。フックが失敗した場合は原因を修正する。
- 秘密情報や巨大な生成物(`node_modules/`、ビルド成果物など)をコミットしない。

### リモートリポジトリ

- リモート: `origin` → https://github.com/shiomiawa/ai-keiei-sanbo.git
- 初期セットアップ(CLAUDE.md の追加・更新)のみ `main` に直接プッシュした。以降は作業ブランチ経由で進める。

## 開発コマンド

(ビルド・テスト・起動などのコマンドが決まったらここに記載する)

```
# 例
# npm install     依存関係のインストール
# npm run dev     開発サーバーの起動
# npm test        テストの実行
```

## ディレクトリ構成

(構成が固まったらここに記載する)
